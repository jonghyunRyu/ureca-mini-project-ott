<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.ureca.web.model.dao.MemberDao">

  <insert id="registerMember" parameterType="Member" >
    INSERT INTO user (user_id, password, email, nickname) 
    VALUES (#{id}, #{password}, #{email}, #{nickname})
  </insert>
  
  <select id="login" parameterType="Member" resultType="Member">
  	SELECT id AS no, user_id AS id, password, email, nickname
	FROM user WHERE user_id=#{id} and password=#{password}
  </select>
  
</mapper>